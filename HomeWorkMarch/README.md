# ПРЕОБРАЗОВАНИЕ массива строк

``` Данная программа преобразовывает массив строк, введенный пользователем на старте работы алгоритма, или из консоли, в массив строк элементов удовлетворяющих условию: размер каждой строки <= 3 символа* ```

Задача выполнена тремя методами и основным кодом без использования контейнеров, операторов Split и Select.

## ОСНОВНОЙ КОД:
   ### БЛОК 1:

         Демонстрация пользователю, заданного на старте, алгоритма:
   1. Создаёт пользовательский массив строк на старте алгоритма;
   2. Методом `ShowArray` выводит в консоль массив строк;
   3. Методом `ArrayRowsThreeElements` преобразовывает пользовательский</br> массив строк в массив строк установленного размера; 
   4. Методом `ShowArray` выводит в консоль массив строк;
   ### БЛОК 2:

         Взаимодействие с кодом пользователя, согласно приведенному ранее образцу, через консоль:
  1. Обращается к пользователю "Ввести массив строк через пробел;
  2. Методом `ConvertToArrayString` принимает строку и возвращает массив строк, удаляя пробелы;
  3. Методом `ArrayRowsThreeElements` преобразовывает пользовательский</br> массив строк в массив строк установленного размера; 
  4. Методом `ShowArray` выводит в консоль массив строк;

### МЕТОД `ConvertToArrayString`:
   1. Принимает параметром строку, введенную пользователем из консоли;
   2. Создает временный массив строк размером длины пользовательской</br> строки и переменные* для организации сбора и отсечки строк,</br> находящихся в диапазоне пробелов;
   3. Записывает результат сбора временной строки во временный массив</br> строк и итерирует индекс временного массива строк на единицу;
   4. Из полученного временного массива с лишним размером ячеек</br> перезаписывает данные в массив строк размерностью заполненных ячеек</br> и возвращает его, как результат работы метода.

### МЕТОД `ArrayRowsThreeElements`:
   1. Принимает параметром массив строк;
   2. Создает временный массив строк размером с пользовательский</br> 
   и переменные* для организации проверки на размерность каждой</br> 
   строки пользовательского массива;
   3. Записывает результат нахождения во временный массив строк и</br>  итерирует индекс временного массива строк на единицу;
   4. Из полученного временного массива с лишним размером ячеек</br> перезаписывает данные в массив строк размерностью заполненных ячеек</br> и возвращает его, как результат работы метода.

### МЕТОД `ShowArray`:
   1. Принимает параметром массив строк;
   2. В случае отсутствия элементов массива выводит символы "*/",</br> иначе выводит полученный массив строк согласно требованиям оформления. 


  
которых меньше либо равна <br/> 3 символа.
итоговая работа

### ПОЯСНЕНИЯ 

- `*` функции переменных отображены в комментариях</br> [link to Program.cs](./Program.cs)
- в целях упрощения проверки работоспособности кода и наглядной </br> демонстрации пользователю того, что от него требуется, пункт "1. ввод </br> массива на старте работы алгоритма" приведен как демонстрационный. </br>
- отдельная обработка любого из вариантов ("1. ввод массива на старте </br> работы алгоритма" или "2. ввод данных из консоли") возможна, если </br> закомменировать первую либо вторую, соответственно часть основного кода; 
- код имеет универсальность - возможность изменения параметров проверки</br> при сортировке массива;
- алгоритм построения решения проблемы прилагается:</br>
[link to Algorithm.drawio.pdf](./Algorithm.drawio.pdf) 
